describe('jest peroframce', function () {
  it('should be fast', function () {
    expect(12).toEqual(12);
  });

  it('should be fast 1', function () {
    expect(12).toEqual(12);
  });
  it('should be fast 2', function () {
    expect(12).toEqual(12);
  });

  it('should be fast 3', function () {
    expect(12).toEqual(12);
  });
});
