// /* eslint-disable jest/no-focused-tests */
// import * as outlier from '../main/pupillary/preprocessing/outlier';

// describe('Dilatation Speed Outlier', () => {
//   describe('Calculate dilatation speed', () => {
//     test('Dilatation speeds include NaN', () => {
//       const dilatationSpeed = [0.01, 0.002, 0.004, NaN, 0.005, NaN];
//       const mean = outlier.calcThreshold(1, dilatationSpeed);
//       console.log(mean);
//     });
//   });

//   describe('Calculate Threshold', () => {
//     test('Dilatation speeds include NaN', () => {
//       const dilatationSpeed = [0.01, 0.002, 0.004, NaN, 0.005, NaN];
//       const mean = outlier.calcThreshold(1, dilatationSpeed);
//       console.log(mean);
//     });
//   });
// });
